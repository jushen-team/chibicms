@using Jushen.ChibiCms.ChibiContent
@model List<ContentMeta>
@{
    ViewData["Title"] = "IndexView";
}

<h2>IndexView</h2>

    @for (int i = 0; i < Model.Count; i++)
    {
        <p>Title: @Model[i].Title</p>
    }




